{
  "name": "open-lyrics",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp src/index.js dist/index.js && cp manifest.json dist/manifest.json",
    "package": "cd dist && zip -j open-lyrics.spotiflac-ext manifest.json index.js"
  }
}
